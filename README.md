# version-control-II--Git-basics-
Using git: The teminal 
when we say "Terminal" we mean the terminal on Mac and Linux, and git bash on Windows 
The terminal is just a way of navigating directories, like finder or Windows explorer
To move around: cd (changee Directories).  "cd Document"
to move more than one level: "cd Documents/example/nested/folder"

git commands = {
    "git clone":"Makes local copy o code in a remote reepository to make changes onto your computer"
    "git add": "Adds a file to the staging area"
    "git commit": "saves the changes made to th file in the staging area" 
    "git push" : "copies the changes made to thee file in the staging area to the remote repository"
    "git branch": " refernces the branch of the project" 
    "git merge" : "merges branches of aa project" 
    "git status": "what git is viewing"
